<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../includes/head') %>
    <link rel="stylesheet" href="/css/index.css">
    <link rel="stylesheet" href="/css/form.css">
</head>
<body>
    <!---------------------------------------- HEADER ---------------------------------------->
    <%- include('../includes/header') %>

    <!---------------------------------------- MAIN ---------------------------------------->
    <main>

        <form action = "/register" method = "post" class = "form" enctype="multipart/form-data">

            <h2 id="title">Completa tus datos!</h2>

            <!-- FIRSTNAME -->
            <fieldset class="">
                <input 
                    type='text' 
                    name="firstName" 
                    placeholder="Nombre" 
                    class= '<%= locals.errors && errors.firstName ? 'pendiente' : null %>'
                    value= '<%= locals.oldData ? oldData.firstName : null %>' 
                >
            </fieldset>
            <% if (locals.errors && errors.firstName) { %>
                <p class="feedbackError">
                    <%= errors.firstName.msg %> 
                </p>
            <% } %>

            <!-- LASTNAME -->
            <fieldset class="">
                <input 
                    type='text' 
                    name="lastName" 
                    placeholder="Apellido" 
                    class= '<%= locals.errors && errors.lastName ? 'pendiente' : null %>'
                    value= '<%= locals.oldData ? oldData.lastName : null %>' 
                >
            </fieldset>
            <% if (locals.errors && errors.lastName) { %>
                <p class="feedbackError">
                    <%= errors.lastName.msg %> 
                </p>
            <% } %>

            <!-- EMAIL -->
            <fieldset class="">
                <input 
                    type='email' 
                    name="email" 
                    placeholder="E-mail" 
                    class= '<%= locals.errors && errors.email ? 'pendiente' : null %>'
                    value= '<%= locals.oldData ? oldData.email : null%>' 
                >
            </fieldset>
            <% if (locals.errors && errors.email) { %>
                <p class="feedbackError">
                    <%= errors.email.msg %> 
                </p>
            <% } %>

            <!-- IMAGE -->
            <fieldset id="loadPicture">
                <label for="image">Fotos:</label>
                <input 
                    type='file' 
                    name="image"  
                    class= '<%= locals.errors && errors.image ? 'pendiente' : null %>'
                    value= '<%= locals.oldData ? oldData.image : null%>' 
                >
            </fieldset>
            <% if (locals.errors && errors.image) { %>
                <p class="feedbackError">
                    <%= errors.image.msg %> 
                </p>
            <% } %>

            <!-- PASSWORD -->
            <fieldset class="">
                <input 
                    type='password' 
                    name="password"
                    placeholder="Contraseña"  
                    class= '<%= locals.errors && errors.password ? 'pendiente' : null %>'
                    value= '<%= locals.oldData ? oldData.password : null%>' 
                >
            </fieldset>
            <% if (locals.errors && errors.password) { %>
                <p class="feedbackError">
                    <%= errors.password.msg %> 
                </p>
            <% } %>

            <!-- PASSWORD CONFIRM -->
            <fieldset class="">
                <input 
                    type='password' 
                    name="passwordConfirm"
                    placeholder="Reingrese la contraseña"  
                    class= '<%= locals.errors && errors.passwordConfirm ? 'pendiente' : null %>'
                    value= '<%= locals.oldData ? oldData.passwordConfirm : null%>' 
                >
            </fieldset>
            <% if (locals.errors && errors.passwordConfirm) { %>
                <p class="feedbackError">
                    <%= errors.passwordConfirm.msg %> 
                </p>
            <% } %>

            <fieldset>
                <button type="submit" name="register" class="button">Registrate!</button>
            </fieldset>

        </form>
    </main>

    <!---------------------------------------- FOOTER ---------------------------------------->
    <%- include('../includes/footer') %>

</body>
</html>